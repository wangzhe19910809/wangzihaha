#include<iostream>
#include<fstream>
#include<string>
using namespace std;

int main()
{
	char *key="abcde";
	int value=0;
	while(*key != '\0')
	{
		value += *key++;
	}
	cout<<"value= "<<value<<endl;
	system("pause");
	return 0;
}
//总结：使用字符型指针char *指向某一个字符串时，指针起始位置是该字符串的第一个字符。该字符型指针可以依次遍历所指字符串的各个字符。
